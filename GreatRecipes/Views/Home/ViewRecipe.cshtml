@model IEnumerable<Recipe>

@{
    ViewBag.Title = "View Recipe";
}

@foreach (var r in Model)
{
    <div style="text-align:center">
        <img src="@r.ImageUrl" />
    </div>
        <h3>@r.RecipeName</h3>
    <br>
    <h4><em>@r.RecipeOwnerName</em></h4>
    <h3>Ingradients</h3>
    <ul>
        @r.Ingradients
    </ul>
    <h3>Directions</h3>
    <ul>
        @r.Directions
    </ul>
    <h3>You are all set; enjoy your meal now</h3>
    <ul style="align-content:center; margin-left:260px">
        <a asp-action="AddReview" asp-route-RecipeId=@r.RecipeId asp-controller="Review"><h4>Add a Review</h4></a>
    </ul>
}
<p></p>
<div style="text-align:center">
    <a asp-action="RecipeList"><h4>View Another Recipe</h4></a>
</div>
